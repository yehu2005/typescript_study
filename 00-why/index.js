"use strict";
// why use typescript? 
// because it's a statically typed language, so it can catch errors at compile time.
const person = {
    name: 'YahooTiger',
    age: 30
};
console.log("my name is " + person.name + ", I am " + person.age + " years old.");
